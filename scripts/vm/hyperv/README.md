# Hyper-V VM Deployment Scripts

> Not yet implemented. See `docs/04-vm-deployment-hyperv.md` for full documented steps and ready-to-implement PowerShell code blocks.

Required module: `Hyper-V` (Windows only — built-in on Server / Win10+ Pro with Hyper-V enabled)
Note: Must run from a Windows host. Cannot execute on macOS.
